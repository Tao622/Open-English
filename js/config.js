/**
 * js/config.js - 部署配置
 *
 * 部署前必填：注册 supabase.com（免费）→ 新建项目 → Project Settings → API
 * 把 Project URL 和 anon public key 填到下面两个引号里。
 * 详见 README.md「用户注册登录」章节。
 */
const SUPABASE_CONFIG = {
  url: '',
  anonKey: ''
};
